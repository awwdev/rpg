# PREFAB LANGUAGE
#   # comment
#   @ prefab 
#   ? component
#   = component data 
#   , component data multiple values
# ONE-LINERS are permitted only
# whitespace is fine

@ Sword
    ? MainComponent
        + Children = Sword_Grib, Sword_Blade

@ Sword_Grib
    ? MainComponent
        + Mesh = sword_grib
        + Material = wood
    
@ Sword_Blade
    ? MainComponent
        + Mesh = sword_blade
        + Material = metallic